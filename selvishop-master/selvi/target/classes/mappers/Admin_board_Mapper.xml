<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="selvishop">
  

  
    <select id="board_list_all" parameterType="hashmap" resultType="hashmap">
        <![CDATA[
          select * from board
        ]]>
    </select>
    

    <select id="board_list" parameterType="hashmap" resultType="hashmap">
        <![CDATA[
          select * from board where board_name = '${board_name}'
        ]]>
    </select>
    
    
  
  </mapper>
  